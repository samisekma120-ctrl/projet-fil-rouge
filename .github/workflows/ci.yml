name: CI - Basic Test

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test runner output
        run: |
          echo "🎉 Le runner self-hosted fonctionne !"
          pwd
